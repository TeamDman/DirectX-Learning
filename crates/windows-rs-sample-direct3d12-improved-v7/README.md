# Goal

We are trying to eliminate the lag between the OS cursor's visible position versus an element on the screen we are dragging.
In lieu of implementing a drag-and-drop system, we can just include a manually rendered cursor and position it under the OS cursor.
Due to many reasons, the manually rendered cursor often lags behind the OS cursor.
We want to eliminate that lag.
